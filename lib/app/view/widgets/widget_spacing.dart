import 'package:flutter/material.dart';

SizedBox spaceingVerical(int size) => SizedBox(
      height: size.toDouble(),
    );

SizedBox spaceingHorizontal(int size) => SizedBox(
      width: size.toDouble(),
    );
